{
  "name": "docker-cache-layers-demo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "node-api:start": "yarn workspace node-api start",
    "node-api:dev": "yarn workspace node-api dev",
    "next-app:build": "yarn workspace next-app build",
    "next-app:start": "yarn workspace next-app start",
    "next-app:dev": "yarn workspace next-app dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
  },
  "devDependencies": {
    
  },
  "author": "",
  "license": "ISC"
}
